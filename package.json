{
  "name": "montycap",
  "version": "1.0.0",
  "description": "Monte Carlo simulation tool for capacity planning",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "test": "vitest run",
    "validate": "npx eslint src/simulation.js src/ui.js && npm run build && npx html-validate web/index.html && npm test",
    "threatmodel": "threatcl validate threatmodel/*.hcl && threatcl dashboard -outdir=docs/threatmodel -overwrite threatmodel/*.hcl && threatcl dfd -outdir=docs/threatmodel -format=svg -overwrite threatmodel/*.hcl",
    "threatmodel:validate": "threatcl validate threatmodel/*.hcl"
  },
  "devDependencies": {
    "eslint": "10.3.0",
    "fast-check": "4.7.0",
    "happy-dom": "20.9.0",
    "html-validate": "10.13.1",
    "papaparse": "^5.5.3",
    "vitest": "4.1.5"
  }
}
